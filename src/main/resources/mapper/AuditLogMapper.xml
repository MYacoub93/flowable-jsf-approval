<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.approval.mapper.AuditLogMapper">

    <!-- One row per clearance audit action (see AuditRecord). -->
    <resultMap id="auditRecordMap" type="com.example.approval.clearance.model.AuditRecord">
        <id property="id" column="ID"/>
        <result property="processInstanceId" column="PROCESS_INSTANCE_ID"/>
        <result property="processName" column="PROCESS_NAME"/>
        <result property="action" column="ACTION"/>
        <result property="stage" column="STAGE"/>
        <result property="department" column="DEPARTMENT"/>
        <result property="user" column="USER_ID"/>
        <result property="initiator" column="INITIATOR"/>
        <result property="taskId" column="TASK_ID"/>
        <result property="details" column="DETAILS"/>
        <result property="timestamp" column="TIMESTAMP"/>
    </resultMap>

    <sql id="allColumns">
        ID, PROCESS_INSTANCE_ID, PROCESS_NAME, ACTION, STAGE, DEPARTMENT,
        USER_ID, INITIATOR, TASK_ID, DETAILS, TIMESTAMP
    </sql>

    <insert id="insertAuditRecord" parameterType="com.example.approval.clearance.model.AuditRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO CLRT_AUDIT_LOG (
            PROCESS_INSTANCE_ID, PROCESS_NAME, ACTION, STAGE, DEPARTMENT,
            USER_ID, INITIATOR, TASK_ID, DETAILS, TIMESTAMP
        ) VALUES (
            #{processInstanceId, jdbcType=VARCHAR},
            #{processName, jdbcType=VARCHAR},
            #{action, jdbcType=VARCHAR},
            #{stage, jdbcType=VARCHAR},
            #{department, jdbcType=VARCHAR},
            #{user, jdbcType=VARCHAR},
            #{initiator, jdbcType=VARCHAR},
            #{taskId, jdbcType=VARCHAR},
            #{details, jdbcType=VARCHAR},
            #{timestamp, jdbcType=TIMESTAMP}
        )
    </insert>

    <select id="findByProcessInstanceId" resultMap="auditRecordMap">
        SELECT <include refid="allColumns"/>
        FROM CLRT_AUDIT_LOG
        WHERE PROCESS_INSTANCE_ID = #{processInstanceId, jdbcType=VARCHAR}
        ORDER BY TIMESTAMP ASC, ID ASC
    </select>

</mapper>